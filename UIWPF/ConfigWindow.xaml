<Window x:Class="TraducaoTIME.UIWPF.ConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configurações"
        Height="400"
        Width="500"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource {x:Static SystemColors.ControlBrushKey}}">
    <StackPanel Margin="20"
            VerticalAlignment="Top">
        <!-- Tipo de Transcrição -->
        <Label Content="Tipo de Transcrição:"
                FontWeight="Bold"
                Margin="0,0,0,5"/>
        <ComboBox Name="ComboBoxTranscricao"
                Height="35"
                Margin="0,0,0,20"
                Padding="10,8"/>

        <!-- Dispositivo de Áudio -->
        <Label Content="Dispositivo de Áudio:"
                FontWeight="Bold"
                Margin="0,0,0,5"/>
        <ComboBox Name="ComboBoxDispositivo"
                Height="35"
                Margin="0,0,0,30"
                Padding="10,8"/>

        <!-- Botão Fechar -->
        <Button Name="ButtonFechar"
                Content="Fechar"
                Height="40"
                Background="#0078D4"
                Foreground="White"
                Click="ButtonFechar_Click"/>
    </StackPanel>
</Window>
